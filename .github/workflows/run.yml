name: Run main
on: push
jobs:
  run:
    name: Job to run main
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-go@v1
        with:
          go-version: '1.14'
      - run: go run main.go

